import React from "react";

const userOutputs=(props)=>{

    return(
    <div className="userOutputs">
    <p>This is first practice assignment</p>
    <p>Let's get started</p>
    <p>Username: {props.userName}</p>
  
    </div>
    )
}

export default userOutputs;